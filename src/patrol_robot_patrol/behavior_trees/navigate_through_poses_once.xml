<!--
  Plan the complete patrol route once and keep following that path. A new plan
  is computed only after FollowPath fails and the recovery branch is entered.
  This avoids replacing a valid controller path every three seconds on slower
  simulation hosts. The controller progress timeout is deliberately short, so
  a genuinely blocked path reaches this failure-triggered replan promptly.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">
      <Sequence name="NavigateWithStablePath">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <RecoveryNode number_of_retries="1" name="ComputePathThroughPoses">
          <ReactiveSequence>
            <!-- Each action contains one auditable inspection task. Do not
                 prune it until the robot is inside the configured goal area. -->
            <RemovePassedGoals input_goals="{goals}" output_goals="{goals}" radius="0.25"/>
            <ComputePathThroughPoses goals="{goals}" path="{path}" planner_id="{selected_planner}"/>
          </ReactiveSequence>
          <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
        </RecoveryNode>
        <RecoveryNode number_of_retries="1" name="FollowStablePath">
          <FollowPath path="{path}" controller_id="{selected_controller}"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </Sequence>
      <RoundRobin name="RecoveryActions">
        <Sequence name="ClearingActions">
          <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
          <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
        </Sequence>
        <Spin spin_dist="1.20"/>
        <BackUp backup_dist="0.20" backup_speed="0.10"/>
        <Wait wait_duration="2.0"/>
      </RoundRobin>
    </RecoveryNode>
  </BehaviorTree>
</root>
