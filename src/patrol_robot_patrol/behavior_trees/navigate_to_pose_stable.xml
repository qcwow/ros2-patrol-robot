<!--
  Each inspection point is a separate NavigateToPose task. The path is planned
  once and followed until completion. A controller failure is returned to the
  patrol manager, which blacklists the remaining route before the next global
  plan. Similar candidates are rejected before reaching the controller.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <!-- Follow NavFn's verified raw path without an in-place smoothing stage,
         so the page and the controller always refer to the same geometry. -->
    <Sequence name="NavigateWithStablePath">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <GoalCheckerSelector selected_goal_checker="{selected_goal_checker}" default_goal_checker="precise_goal_checker" topic_name="goal_checker_selector"/>
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
        <!-- A controller rejection must return immediately. patrol_manager
             then blacklists this exact complete route and asks NavFn for the
             shortest remaining route. Retrying here would follow it again. -->
        <FollowPath path="{path}" controller_id="{selected_controller}" goal_checker_id="{selected_goal_checker}" error_code_id="{follow_path_error_code}"/>
    </Sequence>
  </BehaviorTree>
</root>
